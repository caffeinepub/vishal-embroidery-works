import type { Design } from "../backend.d";

// Sample designs cleared — all designs are now managed via the Admin Panel
export const SAMPLE_DESIGNS: Design[] = [];

export function getSampleTrending(): Design[] {
  return [];
}

export function getSampleBridal(): Design[] {
  return [];
}

export function getSampleNew(): Design[] {
  return [];
}

export function getSampleByCategory(_category: string): Design[] {
  return [];
}
